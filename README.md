# README

Content generate by [hexo-post](https://github.com/liuyanjie/hexo-post), Visit: <https://blog.c--cc.cc/>

Steps:

1. update `hexo.yaml` 文件
2. Run `npx hexo-post -v -c path/to/repo -o .`
3. Run `npx hexo s -g` Generates static files. Starts a local server. Preview.
4. Run `npx hexo d` Deploy to Github.
